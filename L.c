#include<stdio.h>

int main(){
    int t;
    scanf("%d", &t);
    for (int i = 0; i < t; i++)
    {
        int n;
        scanf("%d", &n);
        for (int i = 0; i < n; i++)
        {
            scanf("%d", &n);
        }
        for (int i = 0; i < n - 1; i++)
        {
            for (int j = i; j <= ; j++)
            {
                /* code */
            }
            
        }
        
    }
    
}